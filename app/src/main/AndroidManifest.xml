<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.group15.fitnesstracker">

    <application
        android:allowBackup="false"
        android:name=".FitnessTrackerApplication"
        android:icon="@mipmap/apptitudexxxhdpi"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".dashboard.workout.workoutProgress.WorkoutProgressActivity" />
        <activity android:name=".dashboard.workout.createWorkout.CreateWorkoutActivity" />
    </application>

</manifest>